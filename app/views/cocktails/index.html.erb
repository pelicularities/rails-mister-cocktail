<div class="col">
  <h1>All Cocktails</h1>
  <ul>
	<% @cocktails.order(name: :asc).each do |cocktail| %>
      <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
    <% end %>
  </ul>
</div>