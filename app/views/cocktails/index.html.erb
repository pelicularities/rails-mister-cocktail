<h1>All Cocktails</h1>
<ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= link_to cocktail.name, cocktail_path(cocktail) %></li>
  <% end %>
</ul>
<div>
  <%= link_to 'Create New Cocktail', new_cocktail_path(@cocktail), class: 'btn btn-primary my-3' %>
</div>
