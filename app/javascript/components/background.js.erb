const randomCocktailBackground = () => {
  const maxNum = 11;
  let num = Math.ceil(Math.random() * maxNum);
  const assetPath = `backgrounds/background${num}.jpg`;
  document.body.style.backgroundImage = asset-url(assetPath);
}

export { randomCocktailBackground }
